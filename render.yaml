services:
  - type: worker
    name: video-downloader-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python video_downloader_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
